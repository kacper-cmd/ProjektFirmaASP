@model IEnumerable<Firma.Data.Data.CMS.Strona>
@*wiele rekordow ienumerable to jest partial view, uzywam kolecji stron*@
<div class="nav-wrapper container">
    <a id="logo-container" href="#" class="brand-logo">Logo</a>
    <ul class="right hide-on-med-and-down">
        @foreach (var item in Model)
        {
            <li>
                @Html.ActionLink(item.LinkTytul, "Index", "Home", new  { id = item.IdStrony },null)
            </li>
        }
        @*<li>@Html.ActionLink("Strona glowna","Index","Home")</li>*@
    </ul>

    <ul id="nav-mobile" class="sidenav">
        <li><a href="#">Navbar Link</a></li>
    </ul>
    <a href="#" data-target="nav-mobile" class="sidenav-trigger"><i class="material-icons">menu</i></a>
</div>